"""Pipeline de análise do teste de número redondo (Osler 2000) para o USD/BRL.

Módulos executáveis via ``python -m src.<módulo>`` a partir da raiz do repositório
(ver Makefile / README). Ingestão em ``ingest_mt5``; grades em ``round_levels`` e
``control_levels``; detecção/classificação em ``events``; testes em ``stats``;
drivers em ``run_analysis``, ``event_study``, ``sanity_ptax`` e ``export_web_data``.
"""
